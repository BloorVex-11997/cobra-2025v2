#pragma once

/* Ports */

const int INTAKE_PORT = 2;
const int ELEVATOR_PORT = 9;

/* Configuration */

// voltage applied to intake when intake is enabled [-127, 127]
const int INTAKE_VOLTAGE = 127;

// voltage applied to elevator when intake is enabled [-127, 127]
const int ELEVATOR_VOLTAGE = 127;